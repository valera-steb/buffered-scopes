/**
 * Created on 27.10.2015.
 */
describe('paramsWrapper', function(){
    xit('должен создавать копию объекта по значениям');

    xit('должен копирывать напрямую введённые bufferedScopes-ом объекты');

    xit('должен оборачивать вызовы функций - если они не введённые объекты');
});
